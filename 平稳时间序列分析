工具：
差分运算：
  p阶差分：相距一期的两个序列值之间的减法运算成为1阶差分运算，对一阶差分后的序列再进行一次一阶差分运算后得到的序列称为二阶差分运算，
          以此类推，对p-1阶差分序列作一次一阶差分运算称为p阶差分。
  k步差分：相距k期之间的减法运算称为k步差分运算。
延迟算子：延迟算子类似于一个时间指针，当前序列乘以一个延迟算子之后，相当于把当前序列的时间向过去拨了一个时刻。
线性差分方程：
  齐次线性差分方程：
  非齐次线性差分方程：

ARMA模型性质：
AR模型：
MA模型：
ARMA模型：
